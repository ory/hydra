type MigrationRecord struct {
	Id        string    `db:"id"`
	AppliedAt time.Time `db:"applied_at"`
}

client
jwk
oauth2 -> consent -> client
consent -> client

hydra_oauth2_migration
hydra_client_migration
hydra_oauth2_authentication_consent_migration
hydra_jwk_migration

fizz migration id format YYYYMMDDhhmmss

fizz gets the migration id by scanning for numbers in the 
beginning of the filename -> only decimal digits (any length) considered.
sql-migrate uses the filename without ".sql".
